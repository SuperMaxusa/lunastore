#
# Configuration of LunaStore
#
# All settings below are managed by django-constance.
# Changes made via the admin panel are automatically
# synced back to this file.
#
# Manual edits to this file require an application restart to take effect <3
#

VERSION = "2.7"

ADMIN_URL = ".."

# OpenID (Authentik) config
OIDC_CLIENT_ID = "..."
OIDC_CLIENT_SECRET = "..."
OIDC_ENDPOINT = "..."
OIDC_TOKEN_ENDPOINT = "..."
OIDC_USER_ENDPOINT = "..."
LOGIN_REDIRECT_URL = "..."
LOGOUT_REDIRECT_URL = "..."
OIDC_SIGN_ALGO = "..."
OIDC_JWKS_ENDPOINT = "..."

# Database Configuration
DB_NAME = "..."
DB_USER = "db"
DB_PASSWORD = "..."
DB_HOST = "..." # since postgresql is inside docker, host will be 'db';
DB_PORT = "..."

# Media Configuration
EXTERNAL_MEDIA_URL="http://192.168.1.10/media/"
# Django Configuration
SECRET_KEY='...'

DEBUG = "True" # DEBUG must be "True" if you will setup dev version of lunastore; else, set DEBUG = "False" if you want to setup prod version

# LunaSpire Configuration
LUNASPIRE_SECRET_KEY = "supersecretkey"
LUNASPIRE_URL = "http://192.168.1.10:8080"
LUNASPIRE_URL_WITHOUT_PROTO = "spire.lunastore.app"
API_URL = "http://192.168.1.10:7088"
ADMIN_EMAIL = "sixseven@gmail.com"

# LunaStore Configuration
ALLOWED_HOSTS = "192.168.1.10;192.168.1.1"
# reverse proxies allowed to set CF-Connecting-IP / X-Real-IP / X-Forwarded-For
# example: "10.0.0.0/8;127.0.0.1;172.16.0.0/12"
TRUSTED_PROXIES = ""
CORS_ALLOWED_ORIGINS = "http://192.168.1.10;http://192.168.1.1"
MOTD_LIST = "Windows XP Professional;время подавать напитки и вершить судьбы"
CSRF_TRUSTED_ORIGINS = "https://lunastore.app"

SESSION_COOKIE_DOMAIN = ".lunastore.app"
CSRF_COOKIE_DOMAIN = ".lunastore.app"

# Authorization config
BCRYPT_ROUNDS = 12

# if REGISTRATION_IS_ENABLED = True, we will render register.html on '/register.php' path; if False, we will render register_on.html
REGISTRATION_IS_ENABLED = "True"

# if DEVELOPER_REGISTRATION_IS_ENABLED = True, we will allow users to send dev status requests :3
DEVELOPER_REGISTRATION_IS_ENABLED = "True"

# User Activity Log (include IPs) retention period in days (because storage limitation gdpr; we must store data for the shortest time possible)
RETENTION_ACTIVITY_LOG_DAYS = 0

# if INVITES_ON_REGISTER = True, registration will be accesible only via invites :3
INVITES_ON_REGISTER = "True"
MAX_INVITE_USES_COUNT = 3
MAX_INVITE_DAYS_LIMIT = 7

ENABLE_DRM = "True"
ENABLE_DISTRIBUTION_PROXY = "True"

# SMTP Configuration
EMAIL_HOST='smtp.gmail.com'
EMAIL_PORT='587'
EMAIL_USE_TLS='True'
EMAIL_USE_SSL='False'
EMAIL_HOST_USER='your_email@gmail.com'
EMAIL_HOST_PASSWORD='your_password'
DEFAULT_FROM_EMAIL='your_email@gmail.com'
ADMIN_EMAIL = "sixseven@gmail.com" # email of admin

# Ratelimit config
RATE_LIMIT_ENABLED = "True"
RATE_LIMIT_WINDOW = 25 # count of requests
RATE_LIMIT = 45 # time (in seconds)

# DRF API throttle (format: number/period, period = s|m|h|d)
API_THROTTLE_ENABLED = "True"
API_THROTTLE_ANON_RATE = "1000/hour"
API_THROTTLE_USER_RATE = "5000/hour"

# Logger config
TELEGRAM_LOGGER_ENABLED = True
TELEGRAM_BOT_TOKEN = ...
TELEGRAM_LOG_CHAT_ID = ...
TELEGRAM_LOG_TOPIC_ID = ...
LOG_MODERATOR_LOGINS = "True"

# GlitchTip (Sentry) config
# enable or disable error reporting to GlitchTip ("True" / "False")
SENTRY_ENABLED = "True"
# DSN from your GlitchTip project settings (leave empty to disable)
SENTRY_DSN = ""
# percentage of requests to trace for performance monitoring (0.0 — 1.0)
SENTRY_TRACES_SAMPLE_RATE = 0.2
# percentage of traced requests to profile (0.0 — 1.0)
SENTRY_PROFILES_SAMPLE_RATE = 0.1
# environment tag visible in GlitchTip (e.g. "production", "staging", "development")
SENTRY_ENVIRONMENT = "production"

# ClickHouse analytics (optional; keep False unless you start the analytics profile)
ANALYTICS_ENABLED = "False"
CLICKHOUSE_HOST = "clickhouse"
CLICKHOUSE_PORT = 8123
CLICKHOUSE_USER = "analytics"
CLICKHOUSE_PASSWORD = "analytics"
CLICKHOUSE_DATABASE = "lunastore_analytics"
CLICKHOUSE_SECURE = "False"

# reverse proxy for main domains
GEO_DOMAIN_PROXY_ENABLED="True"
GEO_DOMAIN_OVERRIDES='{
  "RU": {
    "BASE_URL": "ru.lunastore.app",
    "API_URL": "api.ru.lunastore.app",
    "SPIRE_URL": "spire.ru.lunastore.app"
  }
}'
ALLOW_MODERATOR_LOGIN_AS_USER=False

# Meilisearch full-text search
MEILISEARCH_URL = "http://meilisearch:7700"
MEILI_MASTER_KEY = "change-me-in-production"
MEILISEARCH_ENABLED = "True"
